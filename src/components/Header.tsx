import hamburger from '../images/icon-menu.svg';

const Header = () => {
  return (
    <header>
      <h2>snap</h2>
      <img src={hamburger} alt="hamburger" />
    </header>
  )
}

export default Header
